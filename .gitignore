# Never publish secrets or Psiphon-owned config
psiphon_config.json
data/
*.key
conduit_key.json

# macOS junk
.DS_Store

# Private research notes — local reference only, never publish
conduit-iran-notes.md

# End-user protocol drafts — unverified life-safety advice, never publish
# until each claim is checked. Drafts only.
protocols/draft/

# Internal working docs — plan + fact-check output, local reference only
REWORK-PLAN.md
factcheck-ledger.md
